{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the Memo window needs. Kept minimal on purpose: the app only reaches the filesystem through its own Rust commands, so no blanket fs permission is granted to the webview.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
